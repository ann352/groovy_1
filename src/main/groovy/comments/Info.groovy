package comments

class Info {


    //in groovy we have different types of comments - this is single line comment

    /* this
    is multiline comment
     */

    /** groovy doc comment */

    // shebang - !#/usr/bin/env groovy , the first line in whole file, with that comment we can executed code from command line
    //scripts are like other files with .groovy, but they can be executed from commandline
}
