package oop.inheritance

//we also wan't to write slim code, and didn't repeat the same code in many places in that case we can use inheritnace
class Phone {
    String name
    String os
    String appStore

    def powerOn() {
    }

    def powerOff() {
    }

    def ring() {
    }
}
