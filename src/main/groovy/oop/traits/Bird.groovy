package oop.traits

class Bird implements FlyingAbility, SpeakingAbility {
    //thanks to trait we can inherit an implementation of smth that is like Java default methods, or we can also inherit state
    //we can also put some abstract methods to trait - those methods we should implement

    

}
